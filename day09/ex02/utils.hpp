#pragma once

#include <iostream>
#include <algorithm>
#include <vector>
#include <deque>

void   mergeInsertionSortV(std::vector<int> &seq) throw();
void   mergeInsertionSortQ(std::deque<int> &seq) throw();